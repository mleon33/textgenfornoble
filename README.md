echo "# textgeneratorfornoble" >> README.md
git ini
git add README.md
git commit -m "first commit"
git remote add origin https://github.com/mleon33/textgeneratorfornoble.git
git push -u origin master

Everytime you have something new to upload to the remote repository, you can use:

git commit -m "this time I work on: ..... "
git push -u origin master
